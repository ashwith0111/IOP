#include<stdio.h>
int main(){
    int b,h;
    printf("enter the height of the triangle: ");
    scanf("%d",&h);
    printf("enter the length of base of the triangle: ");
    scanf("%d",&b);
    printf("the area is %f",0.5*b*h);
}
